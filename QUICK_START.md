# 🚀 Быстрый старт - Python Class Navigator

## Что это?

Плагин для VS Code, который добавляет иконки и навигацию между родительскими и дочерними классами в Python, как в PyCharm.

## Как запустить?

### 1. Откройте проект в VS Code
```bash
cd /Users/alexeynazarov/Documents/other/vs_plugin
code .
```

### 2. Запустите расширение
- Нажмите **F5** (или `Run > Start Debugging`)
- Откроется новое окно VS Code с активным расширением

### 3. Откройте тестовые файлы
В новом окне:
- `File > Open Folder` → выберите папку `examples` из этого проекта
- Откройте файл `test_inheritance.py`

## Что вы увидите?

### 📍 Иконки в левом поле (gutter):
- 🟢 **Зеленая стрелка вверх** ↑ - переопределяет родительский класс/метод
- 🔵 **Синяя стрелка вниз** ↓ - имеет реализации в дочерних классах
- 🟣 **Фиолетовая стрелка** ↕ - и то, и другое

### 💡 CodeLens (кликабельные ссылки над кодом):
```python
class Animal:          # ← CodeLens: "↓ 6 subclasses" (кликните для перехода)
    def speak(self):   # ← CodeLens: "↓ 6 impls" (кликните для перехода)
        pass

class Dog(Animal):     # ← CodeLens: "↑ Animal" и "↓ 1 subclass"
    def speak(self):   # ← CodeLens: "↑ overrides" и "↓ 1 impl"
        print("Woof!")
```

## Как пользоваться?

### Навигация вверх (к родителю):
1. Кликните на CodeLens **"↑ Parent"** или **"↑ overrides"**
2. Откроется родительский класс/метод

### Навигация вниз (к реализациям):
1. Кликните на CodeLens **"↓ N impls"** или **"↓ N subclasses"**
2. Откроется список всех реализаций
3. Выберите нужную и нажмите Enter

## Примеры

### Пример 1: Класс с дочерними классами
```python
class Animal:  # Кликните "↓ 6 subclasses"
    pass
```
→ Вы увидите: Dog, Cat, Bird, GoldenRetriever, PersianCat, Platypus

### Пример 2: Дочерний класс
```python
class Dog(Animal):  # Кликните "↑ Animal"
    pass
```
→ Переход к классу Animal

### Пример 3: Переопределенный метод
```python
class Dog(Animal):
    def speak(self):  # Кликните "↑ overrides"
        return "Woof!"
```
→ Переход к методу Animal.speak()

### Пример 4: Метод с реализациями
```python
class Animal:
    def speak(self):  # Кликните "↓ 6 impls"
        pass
```
→ Список всех переопределений: Dog.speak(), Cat.speak(), и т.д.

## Структура проекта

```
vs_plugin/
├── src/                        # Исходный код TypeScript
│   ├── extension.ts           # Точка входа расширения
│   ├── pythonParser.ts        # Парсер Python кода
│   ├── classHierarchyProvider.ts  # Иконки в gutter
│   └── codeLensProvider.ts    # Кликабельные CodeLens
├── out/                       # Скомпилированный JavaScript
├── examples/                  # Тестовые Python файлы
│   ├── test_inheritance.py   # Основные примеры
│   └── test_multiple_files.py # Наследование между файлами
├── package.json              # Манифест расширения
└── README.md                 # Документация

```

## Команды

Через Command Palette (`Cmd+Shift+P`):
- **Python Class Navigator: Navigate to Implementations** - переход к реализациям
- **Python Class Navigator: Navigate to Superclass** - переход к родителю

## Разработка

### Компиляция TypeScript:
```bash
npm run compile
```

### Автоматическая компиляция при изменениях:
```bash
npm run watch
```

### Создание .vsix пакета:
```bash
npm install -g @vscode/vsce
vsce package
```

Это создаст файл `python-class-navigator-0.0.1.vsix`, который можно установить через:
`Extensions > ... > Install from VSIX`

## Возможности

✅ **Реализовано:**
- Визуальные индикаторы наследования (иконки в gutter)
- Кликабельные CodeLens для навигации
- Поддержка множественного наследования
- Навигация между файлами
- Неограниченная глубина иерархии
- Автоматическое обновление при изменении кода

🔄 **В планах:**
- Кэширование для повышения производительности
- Поддержка Python Protocol и ABC
- Интеграция с Python Language Server
- Настройки цветов и поведения

## Отладка

Если что-то не работает:
1. Убедитесь, что файл имеет расширение `.py`
2. Убедитесь, что файл в рабочей области VS Code
3. Откройте Developer Tools: `Help > Toggle Developer Tools`
4. Проверьте консоль на ошибки

## Технологии

- TypeScript
- VS Code Extension API
- Регулярные выражения для парсинга Python
- CodeLens API
- Decorations API

---

**Готово! Нажмите F5 и начните использовать!** 🎉
