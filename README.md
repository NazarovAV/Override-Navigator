# Python Class Navigator

Расширение для VS Code, которое добавляет навигацию между родительскими и дочерними классами в Python, аналогично функциональности PyCharm.

## Возможности

- 🔍 **Визуальные индикаторы наследования**: Иконки в левом поле (gutter) показывают:
  - ↑ Зеленая стрелка вверх - метод/класс переопределяет родительский
  - ↓ Синяя стрелка вниз - метод/класс имеет реализации в дочерних классах
  - ↕ Фиолетовая двойная стрелка - и то, и другое

- 🖱️ **Кликабельные CodeLens**: Над каждым классом и методом отображаются кликабельные ссылки для быстрой навигации:
  - "↑ Parent" - переход к родительскому классу/методу
  - "↓ N impls" - переход к реализациям в дочерних классах

- 🚀 **Быстрая навигация**: Клик по CodeLens открывает:
  - Быстрый выбор для перехода к конкретному классу/методу
  - Автоматический переход, если вариант только один

## Использование

1. Откройте любой Python файл с классами
2. Расширение автоматически проанализирует иерархию классов
3. Вы увидите иконки в gutter и CodeLens над классами и методами
4. Кликните на CodeLens для навигации к родительским классам или реализациям

### Пример

```python
class Animal:
    def speak(self):  # ↓ 2 impls - показывает, что метод реализован в дочерних классах
        pass

class Dog(Animal):  # ↑ Animal - переход к родительскому классу
    def speak(self):  # ↑ overrides - переход к родительскому методу
        print("Woof!")

class Cat(Animal):  # ↑ Animal
    def speak(self):  # ↑ overrides
        print("Meow!")
```

## Команды

- `Python Class Navigator: Navigate to Implementations` - Переход к реализациям
- `Python Class Navigator: Navigate to Superclass` - Переход к родительскому классу

## Требования

- VS Code версии 1.80.0 или выше
- Python файлы в рабочей области

## Установка для разработки

1. Клонируйте репозиторий
2. Запустите `npm install`
3. Откройте проект в VS Code
4. Нажмите F5 для запуска в режиме отладки

## Сборка расширения

```bash
npm install
npm run compile
```

Для создания .vsix пакета:
```bash
npm install -g @vscode/vsce
vsce package
```

## Известные ограничения

- Анализ работает только для Python файлов в текущей рабочей области
- Не поддерживаются динамические классы и метаклассы
- Импорты из внешних библиотек не анализируются

## Планы развития

- [ ] Поддержка множественного наследования
- [ ] Кэширование результатов анализа для повышения производительности
- [ ] Поддержка интерфейсов (Protocol, ABC)
- [ ] Интеграция с Python Language Server для более точного анализа

## Лицензия

MIT

## Автор

Создано для удобной навигации по Python коду в VS Code
