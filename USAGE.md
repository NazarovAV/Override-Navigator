# Инструкция по использованию Python Class Navigator

## Быстрый старт

### 1. Запуск расширения в режиме разработки

1. Откройте проект в VS Code
2. Нажмите `F5` или выберите `Run > Start Debugging`
3. Откроется новое окно VS Code с активированным расширением

### 2. Тестирование функциональности

1. В новом окне откройте папку `examples` из этого проекта
2. Откройте файл `test_inheritance.py`
3. Вы увидите:
   - **CodeLens** над классами и методами (например, "↑ Animal", "↓ 3 impls")
   - **Иконки в gutter** (слева от номеров строк):
     - 🟢 Зеленая стрелка вверх - переопределяет родительский метод/класс
     - 🔵 Синяя стрелка вниз - имеет реализации в дочерних классах
     - 🟣 Фиолетовая стрелка - и то, и другое

### 3. Навигация

#### Переход к родительскому классу/методу:
- Кликните на CodeLens "↑ Parent" или "↑ overrides"
- Или поставьте курсор на строку и выполните команду "Navigate to Superclass"

#### Переход к дочерним классам/реализациям:
- Кликните на CodeLens "↓ N impls" или "↓ N subclasses"
- Или поставьте курсор на строку и выполните команду "Navigate to Implementations"

### 4. Примеры использования

#### В файле `test_inheritance.py`:

**Класс Animal (строка 6):**
- Имеет CodeLens: "↓ 6 subclasses" (Dog, Cat, Bird, GoldenRetriever, PersianCat, Platypus)
- Кликните, чтобы увидеть список всех дочерних классов

**Метод Animal.speak() (строка 13):**
- Имеет CodeLens: "↓ 6 impls" (все дочерние классы переопределяют этот метод)
- Кликните, чтобы перейти к любой реализации

**Класс Dog (строка 22):**
- Имеет CodeLens: "↑ Animal" (наследуется от Animal)
- Имеет CodeLens: "↓ 1 subclass" (GoldenRetriever)
- Кликните на "↑ Animal" чтобы перейти к родительскому классу
- Кликните на "↓ 1 subclass" чтобы перейти к GoldenRetriever

**Метод Dog.speak() (строка 25):**
- Имеет CodeLens: "↑ overrides" (переопределяет Animal.speak)
- Имеет CodeLens: "↓ 1 impl" (GoldenRetriever также переопределяет)
- Двойная навигация: вверх к родителю и вниз к дочерним

**Класс GoldenRetriever (строка 51):**
- Имеет CodeLens: "↑ Dog" (наследуется от Dog)
- Кликните для перехода к Dog, откуда можно перейти к Animal

## Команды VS Code

Вы можете использовать Command Palette (`Cmd+Shift+P` или `Ctrl+Shift+P`):

1. `Python Class Navigator: Navigate to Implementations`
2. `Python Class Navigator: Navigate to Superclass`

## Возможности

### ✅ Реализовано:
- Визуальные индикаторы наследования в gutter
- Кликабельные CodeLens для навигации
- Поддержка множественного наследования
- Навигация между файлами в рабочей области
- Иерархия классов любой глубины
- Переопределенные методы
- Автоматическое обновление при изменении кода

### 🔄 Работа в процессе:
- Кэширование для улучшения производительности
- Поддержка Python Protocol и ABC
- Интеграция с Python Language Server
- Поддержка @property и декораторов

## Отладка

Если расширение не работает:

1. Проверьте, что файл имеет расширение `.py`
2. Проверьте, что файл находится в рабочей области
3. Откройте Developer Tools: `Help > Toggle Developer Tools`
4. Проверьте вывод в консоли на наличие ошибок

## Сборка .vsix пакета

Для создания установочного пакета:

```bash
npm install -g @vscode/vsce
vsce package
```

Это создаст файл `python-class-navigator-0.0.1.vsix`, который можно установить:
- `Extensions > ... > Install from VSIX`

## Технические детали

### Архитектура:

1. **extension.ts** - Точка входа, регистрация команд и провайдеров
2. **pythonParser.ts** - Парсинг Python кода, поиск классов и методов
3. **classHierarchyProvider.ts** - Управление декорациями (иконки в gutter)
4. **codeLensProvider.ts** - Предоставление кликабельных CodeLens

### Как это работает:

1. При открытии Python файла расширение парсит его с помощью регулярных выражений
2. Находит все классы и их методы
3. Анализирует наследование (через `class Name(Parent):`)
4. Ищет реализации в других файлах рабочей области
5. Создает CodeLens и decorations для визуализации
6. При клике выполняет навигацию к нужному файлу и строке

### Производительность:

- Парсинг выполняется асинхронно
- Поиск по файлам использует VS Code API (vscode.workspace.findFiles)
- При изменении документа происходит повторный анализ только этого файла

## Дополнительная настройка

В будущих версиях можно будет настроить:
- Цвета иконок
- Включение/выключение CodeLens
- Включение/выключение иконок в gutter
- Глубину поиска наследования

## Обратная связь

Если вы нашли баг или хотите предложить улучшение, создайте issue в репозитории проекта.
